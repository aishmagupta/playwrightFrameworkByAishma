{
  "name": "playwright-framework-by-ai",
  "version": "1.0.0",
  "description": "Playwright framework with Page Object Model and fixtures",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:run:test:sauce-demo": "playwright test tests/sauce-demo.spec.ts "
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.6.0"
  }
}
